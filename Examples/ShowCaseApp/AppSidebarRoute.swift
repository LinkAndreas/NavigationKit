import Foundation

/// The single route hosted by the sidebar column on iPad — a static list of the app's
/// top-level features. Selecting an item swaps the split navigator's detail column.
enum AppSidebarRoute: Hashable {
    case menu
}
